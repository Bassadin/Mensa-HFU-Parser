export function dateToDateTabString(date: Date) {
    const dayNumber = date.getDate();

    // const prefix: string = "tab-";

    // let suffix: string;

    switch (dayNumber) {
        case 1:

            break;
    }

    return "test";
}